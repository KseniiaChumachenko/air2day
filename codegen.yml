overwrite: true
schema: "./schema.graphql"
documents: "**/*.graphql"
config:
  noNamespaces: true
generates:
  src/generated/graphql.tsx:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo

